module.exports = class ScenarioDto{

    constructor(description,options, requests) {
        this.description = description;
        this.options = options;
        this.requests = requests;
      }
};